# W09 / W12 / W13 Final Rebuild Audit

Date: 2026-09-17. Owner: Tab1 SystemModule worker.

Status: frontend implementation and focused verification handed off. No further worker task or test process is running. This is not a claim of 100% end-to-end business coverage. Residual dependencies and unverified cases are listed below.

## Changed Files And Boundaries

- `frontend/web/js/modules/system.js`: rebuilt W09/W12/W13 using existing jQuery and DevExtreme, actual REST data, role/branch checks, forms, loading/empty/error states and validation.
- `docs/reports/tab1-web-admin/rebuild-system-audit.md`: this final report.
- No backend, shared SDK, ERD, US, mobile, shell, CSS or mailbox edits by this worker. Existing changes by other workers remain untouched. Servers on ports 3000/5000 remain running.

## Reviewed Sources

Read Main Flow, all Alternate/Exception Flows, Activity Diagram and every Field-level specification for the 11 stories below; corresponding QTV W09/W12/W13 and LT W09 epics; relevant Product Spec sections 4.6/4.9/4.10; `.agents/rules/ui-design-system.md` and `.agents/rules/docs-sync.md`.

`docs/ui-related-screen-audit.md` now exists with the coordinator's 65 links; the earlier missing-file note is resolved. The worker did not change US; the coordinator subsequently clarified W12-US01 configuration versus observed connectivity fields, with matching Epic/Product Spec wording. The user's explicit no-default-send decision is synchronized in W09-US01 Main Flow/Activity Diagram, Epic and Product Spec. W13 uses the canonical US/Product Spec four-field edit form despite extra fields mentioned in the older epic overview.

## Integration Exports

Preserved `SystemModule.renderNotifications`, `renderEquipment`, `renderRbac`, each accepting `(containerId, context={})` and returning its render Promise. Added `openRecognition(memberId)`, `openRevokeConsent(memberId)`, `openDeviceStatus(deviceId)`, `openIncident(deviceId?)`, and `refresh()`.

External helpers fetch `/auth/me` and actual member/device/catalog context independently, including when invoked from MembersModule or W07. Consent changes emit `paradise:consent-updated` with `{member_id}` from the member integration helpers. Notifications `context.action='create'` opens the template form; equipment `action='create'` opens the device form; equipment `member_id` opens the consent view. W12 root remains QTV-only; LT device status/report access is through the W07 helpers.

Preserved shell hooks `.view-header`, `.view-header-title`, `.view-actions`, `.card-panel`, `.filter-bar`, status badge classes. No optional WebUI dependency. No business mock data, fabricated token, default account, simulated device success or manual notification composer was added.

## Confirmed Contracts And Resolved Earlier Gaps

The original requested endpoint catalog is superseded by the implemented contracts in `docs/reports/tab4-backend-db/web-rebuild-api-contracts.md` and current backend source. Requests use `apiClient.request`, authenticated active role and selected branch; server remains authoritative for authorization and persistence.

| Area | Actual contract used |
|---|---|
| Notifications | `/notifications/events`, `/notifications/rules`, `/notifications/templates`, `/notifications/templates/:id`, `/notifications/history`. Rule PUT targets `/notifications/rules` with `event_type`, branch/template, `recipient_roles`, `modes:DIRECT/BRANCH`, `is_enabled`. Template writes use `template_name,event_type,title_template,body_template`; status changes use `is_active`. History uses `date_from,date_to,event_type,is_read,q,page,limit`. |
| Devices/incidents | `/devices/catalog`, `/devices`, `/devices/:id`, `/devices/:id/test`, `/devices/:id/events`, `/devices/incidents`, `/devices/incidents/:id`. Actual fields include `location_description,enabled,last_heartbeat`; capability catalog controls direction and connection fields. |
| Consent | `/consents/catalog`, `/members/:id/consents`, `/members/:id/biometric-enrollment`, `/members/:id/recognition/test`, `/members/:id/recognition/ready`, `/members/:id/revoke-biometric`. Consent writes use `consent_type,is_granted,consent_version,identity_verified`; public-display revoke uses `is_granted:false`. Latest consent records are selected by type. |
| Accounts/audit | `/accounts`, `/accounts/stats`, `/roles`, `/branches`, `/accounts/:id`, `/audit-logs`. Account writes use `status,roles,branch_ids,is_all_branches`. Stats accept `roles/role_counts`; authorized branch names are joined when account rows contain only branch IDs. |

Resolved: LT can read the event catalog; device details now provide branch and nullable updater names; incident rows include branch names; consent audit includes actor and policy version; audit date/action/query filters are implemented. Device events identify `source:ADMIN_AUDIT`; UI labels that source explicitly rather than presenting it as physical telemetry. Unconfigured rules come from the API event catalog and are labeled unconfigured, not invented enabled rules.

## Per-Story Traceability

Every row below includes review of the written flows, diagram and field tables. "Implemented" describes the source; "checked" describes actual worker execution. Server-only checks and coordinator results are not represented as worker browser tests.

| Story | Audited / implemented behavior and fields | Actual worker checks | Residual gaps / NOT verified |
|---|---|---|---|
| QTV-W09-US01 - Automatic rules | Event name/code, recipients, template, channel, enabled toggle; right-side edit form with readonly event/channel, recipient-role and delivery-mode checkboxes, active matching templates; requires nonempty roles/modes and branch for new configuration; toggle submits full configuration and reverts on error. Automatic event sending only. | Real event/rule/template catalogs loaded; unconfigured entries rendered; forced rules request failure showed error/retry, retry recovered actual rows. | Rule save/toggle persistence, failed-toggle rollback and delivery triggered by every event were NOT worker-browser verified. Parent/backend integration coverage is separate. |
| QTV-W09-US02 - In-app templates | Query/event filters, code/name/event/title/status/actions; required name/event/title/body with 100/150/1000 limits, event-dependent Vietnamese variable buttons, cursor insertion, unknown-variable validation, API-generated code, create/edit/status actions. | Blank form rejected; actual catalog used; insertion at index 5 produced `Chao {{member_name}}ban`; invalid variable rejected without POST. | Happy-path create/edit/deactivate persistence was NOT worker-browser verified. Legacy `PAYMENT_COMPLETED` template event is absent from canonical catalog; some historical names/creators remain null. |
| QTV-W09-US03 - Notification history | Today-default required date range, event/read/query filters; time, event, two-line recipient, title, reference, read state, full-text detail; allowlisted source-document REST detail. No mark-read mutation. | QTV history page and actual REST-backed grid loaded; filter/detail implementation reviewed. | Nonempty history detail/source-link traversal and all date/read filter combinations were NOT independently worker-browser verified. |
| QTV-W09-US04 - Template detail | Right-side detail with code/name/event/status/channel, creator/date, title/body; API-variable labels in body; edit transition and closing without rebuilding the template table. | Actual stored template opened; readonly detail and Vietnamese legacy-variable labels checked. | Every template variant and preserving a nondefault filter/page across detail-to-edit-to-save were NOT worker-browser verified. Null legacy creator remains displayed as unavailable. |
| LT-W09-US01 - Branch history | History-only tabs, same date/event/read/query and readonly detail fields; branch/recipient visibility enforced by API. No config, template editing or mark-read controls. | Actual LT session displayed history only with zero config/template tabs. LT event-catalog access fixed and confirmed by parent live checks. | Exhaustive cross-branch/member/PT recipient isolation and source-document navigation were NOT independently worker-browser verified; parent/backend scope tests apply separately. |
| QTV-W12-US01 - Device management | Registry grid; required unique code, name/type, scoped branch/location, capability-dependent direction/status; conditional connection fields; readonly heartbeat/test/updater; immutable edit code; move confirmation, test and soft deactivate; permission-aware mutation CTAs. Coordinator corrected status prefill/save, readonly current connectivity, API-catalog status options, optional IP clearing and two-column desktop form. | Actual devices/catalog loaded; edit code readonly; FACE_CAMERA showed IP, hid endpoint, directions IN/OUT/BOTH; real device test returned 503 and UI showed unavailable, not success. Explicit permission-false fixture hid add/edit/test/deactivate. Coordinator's focused 12 checks passed, including failed-save preservation and 768/390px form bounds. Backend isolated tests verify persistence, reenable, audit and no fabricated telemetry. | No live device record was mutated in the browser. Full move/duplicate-code browser workflow and physical adapter test remain unverified. |
| QTV-W12-US02 - Recognition consent | API member identity, purpose/version readonly; explicit consent and identity verification before capture; online capture-capable device selection; guarded capture/test/READY sequence, busy protection, truthful provider error; member avatar separate. Member integration loads its own context. | Actual member/purpose/version dialog opened from exported helper; actual consent state returned NOT_ENROLLED/NONE; form/API paths reviewed. | Consent grant/capture/test/READY success and failed-capture recovery were NOT worker-browser executed. Trusted biometric provider is unavailable; no READY is fabricated. |
| QTV-W12-US03 - Revoke consent | Latest active recognition/public/birthday consent options, member identity, distinct impacts, confirmation, recognition-only optional deletion request; public revoke does not call biometric revoke; consent/deletion/audit state from API; 12-month access-log retention impact shown. | Actual member consent context fetched; empty-active-consent case observed; helper and conditional request paths reviewed. | No active consent was revoked on live member data. Recognition versus display isolation, deletion completion and retention were NOT worker-browser verified. Backend integration tests cover consent separately; physical deletion remains a dependency. |
| QTV-W12-US04 - Status/incidents | Actual status/heartbeat/error and device event times/source/backfill; incident device/description/severity/status; new incident OPEN readonly, LT cannot update progress, QTV permission required to edit, existing device immutable. | Device detail and unavailable test inspected; new QTV incident OPEN/readonly and blank invalid; LT new incident OPEN/readonly and blank invalid; LT direct W12 denied. | Incident create/update persistence and physical offline/restore/backfill/K01 behavior were NOT worker-browser verified. Current event endpoint provides admin audits, not trusted device telemetry. |
| QTV-W13-US01 - Accounts/KPIs | Four KPI totals, query/role-count/status filters/reset; login, name/profile, role badges, branch, status, edit; page sizes 10/20/50; role-denied entry redirects overview, API errors shown; scoped actual data and readonly audit tab. | 13 actual accounts rendered; desktop 1440 and mobile 390 screenshots inspected; mobile document width exactly 390; read/list baseline and edit CTA visible with actual QTV permission; explicit-false fixture hid edit. | Full paging/filter combinations and real restricted-QTV read session were NOT worker-browser verified. Backend requires manage_accounts even for account reads, so false may show an authorization error rather than a readonly account list. |
| QTV-W13-US02 - Edit account | Exactly login_phone readonly, status required, roles required multiselect, branch_scope conditional for staff; all-system scope offered only to authorized operator; preserve existing multi-branch scope; local self-protection plus authoritative server last-admin/session guards; actual PUT then refresh. | Four fields confirmed; phone readonly; empty roles invalid; MEMBER hides branch, PT shows branch; self-lock blocked without PUT. Permission-false fixture hides edit CTA; function also guards writes. | Successful account mutation, real last-admin race and session revocation were NOT worker-browser executed to avoid changing live accounts; parent/backend tests cover those server behaviors separately. |

## Actual Verification Results

- Final `node --check frontend/web/js/modules/system.js`: PASS.
- Authenticated real API GETs for event/rule/template/device catalogs, devices, incidents, accounts/stats/roles: HTTP 200. Real password and server-issued development 2FA used; no invented token, no logout or business seed changes.
- Final focused permission run: actual permission-true baseline CTAs present; response fixture setting only `manage_devices:false` / `manage_accounts:false` hid corresponding mutation CTAs while device reads still rendered. This verifies frontend gating, NOT server persistence of the fixture permissions.
- Forced notification-rules network failure displayed an error and retry; removing interception and retrying restored real rows: PASS.
- New incident OPEN/readonly and empty-invalid: PASS. Actual device-test POST: HTTP 503, truthful unavailable message: PASS. No fabricated heartbeat/readiness.
- Final desktop/mobile account render: PASS, no document overflow at 390px, zero `pageerror` events. Screenshots inspected at `%TEMP%/paradise-system-accounts-final.png` and `%TEMP%/paradise-system-accounts-mobile-final.png`. Earlier notification/device/template/form screenshots also remain in OS temp.
- An intermediate test harness used an incorrect DevExtreme instance selector after the functional assertions; the final pending screenshot/layout check was rerun successfully without that selector. No unfinished assertion is counted as PASS.
- Parent independently reports **213 backend HTTP checks, 63 live menu/viewports across three viewport sizes, 24 operation checks and 12 focused W12/W07 checks PASS**. These are coordinator-provided totals, not additional worker runs or proof that every hardware/provider scenario passed. The last backend run covers the user's configured-only notification decision, and the W09 diagram parses successfully.
- Worker test browsers/processes are closed. No worker test session remains running; shared servers 3000/5000 were not stopped.

## Residual Handoff

1. W12 status write contract is resolved: `configured_status` preserves the validated saved choice; readonly `connection_status` reflects heartbeat/telemetry; `status` is effective operational state. Editor prefills configured status, list/gate readiness uses effective status. Manual Online cannot fabricate a fresh heartbeat. Twelve focused parent browser checks are recorded in `%TEMP%/paradise-device-status-checks.json`; one is explicitly an isolated response case, not live telemetry.
2. Physical device capture, connection success, recognition READY, backfill ingestion and K01 replay suppression remain NOT verified without the trusted provider. Current events are explicitly admin-audit records.
3. Legacy notification template event/name/creator gaps are real stored-data issues, not missing endpoints. Backend intentionally preserved historical records; normalize/migrate only through the backend owner.
4. W13 account read authorization is stricter than a readonly QTV page: backend requires `manage_accounts`; mutation CTAs and editor guards honor explicit false. A separate read permission would require a backend contract change.
5. Per-story happy-path writes not executed in this worker's live browser are marked NOT verified above. Parent/backend test totals do not replace provider integration or an exhaustive per-US browser acceptance run.
6. The user explicitly resolved the concurrent notification-policy question: send only after QTV saves an enabled rule, as W09-US01 specifies. No rule, stored OFF rule, or inactive configured template must suppress delivery. Newly added event types remain available; unconfigured UI rows are not represented as sending. The final backend handoff records regression evidence for this decision.

Final handoff is complete within the assigned SystemModule/report ownership. No additional feature work is pending from this worker.
